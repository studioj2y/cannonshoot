# GAME_DESIGN.md —— 数值与长期游玩设计方案

> 版本 **v1.3** · 2026-09-24 · 状态：**第 8 节 5 条决策已拍板；第①②层已落地（挑战模式 / 四种弹药 / 解锁门槛 / 进度持久化）；第③④层未做；第 5 节的数值重配平未落地**。v1.2 追加：**爆破筒改为接触引爆**（第 10 节，含前后实测对比）；v1.3 追加：**爆炸表现层重做**（第 11 节 —— 无数值变化，只补一条「表现段 / 物理段分界线」的纪律）；v1.4 追加：**L7 挑战积分线 850 → 1800**（旧公式漏算连击奖励，实测只相当于 23% 发挥）
> 触发：阿光「这个游戏现在的问题是重复感强，挑战不足，很难吸引长期游玩」
> 方法：本机技能 `game-numeric-design`（《游戏数值设定入门》五特性模型 + 数值反推流程）
>
> 本文是**数值层面的权威源**，与另外三份文档的分工：
> - `README.md` —— 现状说明书（怎么玩、有哪些功能）
> - `HANDOVER.md` —— 接手书（代码怎么读、踩过哪些坑）
> - `ART_DIRECTION.md` —— 美术方向单
> - **`GAME_DESIGN.md`（本文）** —— 为什么这么设计、数值该怎么定、下次改动的判据
>
> 落地任何一条之前先读第 3 节「判据」，落地之后在**第 9 / 10 / 11 节**补实测。
>
> ⚠️ **实际落地的数值以 `src/game/ammo.ts` 为准**，第 9.2 节记了「v1.0 提案 → 实际实现」的每一处偏差 —— 别照第 ② 节那张提案表去改代码。

---

## 1. 诊断：三个根因，全部有数据支撑

### 1.1 关键度量：理论最少发数 `E`

```
E = 目标 ÷ 一发最好
```

- `一发最好`：用 `tools/playtest.mjs` 穷举候选弹道，扫出的**单发最大战果**（砖块数 / 目标物数）。
- `E < 1` 意味着**存在一条「一发即胜」的弹道**。玩家只要找到它，关卡就结束了。
- 所以 `E` 不是「难度」，而是**难度存在的必要条件**：`E ≤ 1` 的关卡，其全部挑战都落在「找不找得到那条线」上，一旦找到就退化为背诵题 —— 这正是重复感的物理来源。

### 1.2 现状实测（拉近靶场后，`FIELD_SHIFT_Z = 6`）

| 关 | 目标形态 | 可击倒 | 一发最好 | 现目标 | **E** | 判读 |
|---|---|---|---|---|---|---|
| L1 | 数量 8 | 26 | 20 | 8 | **0.40** | 半发即胜 |
| L2 | 目标物 ×1 | 18 | 19 | 1 | **≤1** | 只要打得中就赢，最脆 |
| L3 | 数量 14 | 22 | 21 | 14 | **0.67** | 单发覆盖 95%，本质一发关 |
| L4 | 红砖 ×4 | 16 | 9 | 4 | **0.44** | 半发即胜 |
| L5 | 数量 30 | 51 | 41 | 30 | **0.73** | |
| L6 | 数量 18 | 38 | 26 | 18 | **0.69** | |
| L7 | 目标物 ×2 | 36 | 14 | 2 | **2.0** | 两物分列左右，形态上已合格 |
| L8 | 数量 19 | 38 | 38 | 19 | **0.50** | 单发覆盖 **100%**，一塌全塌 |
| L9 | 数量 12 | 54 | 17 | 12 | **0.71** | 结构最散，单发覆盖仅 31% |
| L10 | 数量 28 | 65 | 47 | 28 | **0.60** | 单发覆盖 72% |

> ⚠️ **度量本身的盲区（必须知道）**：`一发最好` 数的是**砖块数**，对「打目标物」类关卡会失真 —— L7 有两个目标物分列 x=−7 与 x=+7，一发物理上不可能同时命中，实际最少 2 发。所以 L7 的 0.14（按砖数算）是假象，**真实 E = 2**。L2 只有 1 个目标物、暴露在外，**真实 E ≤ 1**，是真脆。
> 下次跑 `playtest.mjs` 时，对 target 类关卡应单独看「一发最多打掉几个目标物」。

**八个「数量/颜色」目标关的平均 E = 0.56。** 也就是：**一关的目标，半发好球就完成了。** 5~6 发弹药不是难度，是「找不到弹道时的试错预算」。

### 1.3 根因

**根因一 · 挑战在「找弹道」，不在「执行计划」**
E < 1 ⇒ 一发即胜。玩家的全部决策被压缩成一次弹道搜索；找到之后关卡不再提供任何新信息。**没有「第二发依赖第一发的后果」这种结构，就不可能产生「计划」。**

**根因二 · 难度曲线是噪声，不是曲线**
E 序列 0.40 / 0.05 / 0.67 / 0.44 / 0.73 / 0.69 / 0.14 / 0.50 / 0.71 / 0.60 与关卡序号几乎零相关。玩家感知不到自己在变强，因为**游戏没有在变难**。

**根因三 · 星级系统空转（数值 bug 级）**

| 问题 | 证据 | 状态 |
|---|---|---|
| **L1 三星 = 二星** | 目标 8 块 = 800 分，剩 2 发奖励 +400 ⇒ 满足 2★ 时 `score ≥ 900` **必然成立** | 未修（普通模式星级判定本轮未动） |
| **L4 三星数学上要靠运气** | 目标 4 块红砖 = 400 分；把 16 块可击倒砖**全部**打倒也才 1600 + 400 = 2000 < `targetScore` 2600 ⇒ 只能靠一次性全塌的连击奖励硬堆 | 未修 |
| **`targetScore` 不单调** | 900 / 1600 / 2000 / 2600 / **4200** → **2200** / 2000 / 2400 / **1500** / 3800（L5 最难拿星，L9 最松） | 未修 |
| **弹药奖励混进三星判定** | `score` 里含 `ammo × 200`，门槛被剩弹抬着，导致上面两条 | 未修 |
| **星级不持久化** | `App.tsx` 的 `best` 只是 `useState`，刷新即清零 | ✅ **已修**（2026-09-24）：落在 `src/progress.ts` 的 `stars`，键 `cbc:progress:v1` |
| **无关卡解锁门槛** | 暂停面板可直跳任意关 | ✅ **已实现**（2026-09-24）：决策 #1 取 B，`isUnlocked` + 选关面板 `disabled` + 🔒「未解锁」 |

> ⚠️ 星级那三条**没有修**。本轮走的是决策 #3 的另一条路：**不动普通模式的星级判定，另起一套独立的挑战模式判定**（见第 9.1 节）。所以「L1 三星 = 二星」「L4 三星靠运气」目前**依然成立** —— 想修还得按第 4 节 ① 的做法来。

**根因四 · 两个「不变」叠加 ⇒ 一遍即穷尽**
关卡布局固定（打完就没了）+ 玩家手里永远只有一颗标准球（能力不横向上长）。玩过一遍 ≈ 玩过全部。

### 1.4 五特性模型照检（技能方法论）

| 特性 | 现状 | 结论 |
|---|---|---|
| 生成 | 10 关硬编码，无再生 | ❌ 一次性 |
| 成长 | 玩家技巧被「E < 1」封顶 —— 一发就是天花板 | ❌ 无成长空间 |
| 消亡 | 打完 10 关内容耗尽，无淘汰与替换 | ❌ 无节奏 |
| 变化 | 每局完全相同（同结构、同物理、同弹药） | ❌ 零方差 |
| 联系 | 挑战与奖励之间无连接（星级不存、不展示） | ❌ 断链 |

**五个特性四个残缺。**这不是手感问题，是数值模块缺了一半 —— 所以解药也必须分层补，不能只调数字。

---

## 2. 设计目标（可验证）

| 目标 | 度量 | 现状 | 目标值 |
|---|---|---|---|
| 让「一发即胜」在中后期消失 | `E` 曲线 | 平均 0.56，非单调 | **单调递增：0.8 → 2.0** |
| 让每关有多个可追求目标 | 每关可达成目标数 | 1（仅通关） | **≥ 4**（3 星 + 成就徽章） |
| 让进度可积累 | 持久化 | 无 | **localStorage + 首页总进度** |
| 让同关可重复游玩有理由 | 重玩动机 | 无 | **3★ = 理论最少发数内完成** |
| 让内容可再生 | 关卡来源 | 硬编码 10 关 | **种子生成器（M4）** |
| 让打法不唯一 | 弹药种类 | 1 | **4** |

---

## 3. 判据（改数值前必须遵守）

> 这一节是本文最有价值的部分。以后任何人改 `levels.ts` 的 `targetScore` / `objective.count` / `ammo`，都按这三条走，不要凭手感。

### 判据一 · 目标数由 `E` 反推，不手填

```
目标数 = round(一发最好 × 设计E)
约束：目标数 ≤ floor(可击倒 × 0.95)        ← 必须留至少 5% 容错，否则等于要求全清
```

**设计 E 曲线**（本文的核心主张）：

| 关 | L1 | L2 | L3 | L4 | L5 | L6 | L7 | L8 | L9 | L10 |
|---|---|---|---|---|---|---|---|---|---|---|
| 设计 E | 0.8 | 2.0 | 0.95 | 2.0 | 1.05 | 1.3 | 2.0 | 1.5 | 1.8 | 2.0 |
| 段位 | 教学 | 正戏 | 教学 | 正戏 | 进阶 | 进阶 | 正戏 | 进阶 | 高手 | 高手 |

- **E < 1（教学段）**：允许一发通关，用于教会机制。
- **E ≈ 1（正戏段）**：一发**刚好不够**，必须打出一发好球 + 一点连带。这是「挑战感」的起点。
- **E > 1.3（高手段）**：必须做计划 —— 第二发的价值取决于第一发改变了什么。

### 判据二 · `E > 1.3` 需要「单发覆盖率 ≤ 65%」

这是本方案最重要的**结构约束**。因为 `目标 ≤ 可击倒 × 0.95`，所以：

```
E_max = 0.95 × 可击倒 ÷ 一发最好
```

| 关 | 单发覆盖率 | `E_max` | 能否做到 E = 2.0 |
|---|---|---|---|
| L3 | 95% | 1.00 | ❌ 不可能 |
| L8 | **100%** | 0.95 | ❌ 不可能 |
| L10 | 72% | 1.32 | ❌ 不可能 |
| L5 | 80% | 1.18 | 接近上限 |
| L6 | 68% | 1.39 | 勉强 |
| L9 | 31% | **3.03** | ✅ 余量很大 |

**结论：L3 / L8 / L10 想变难，只能改结构，不可能靠调数字。** 这三关的单发覆盖率过高（一塌全塌的连锁结构），必须拆成多个**独立承重单元**（`tower` 分开、去掉把两侧连起来的横板），让一发只能吃掉其中一个单元。改法参照 L9 的「双砦 + 中廊」和 L6 的教训（见 `projects/color-brick-cannon.md`）。

### 判据三 · 弹药是「试错预算」，随结构复杂度增长，不随 E 增长

```
弹药 = ceil(设计E) + 试错预算
试错预算：3（单一结构）/ 4（2~3 个单元）/ 5（4 个以上单元）
```

理由：`E` 衡量的是**要做多少事**，试错预算衡量的是**要试多少次才能找到好弹道**，后者取决于结构数量和散布程度，与 `E` 无关。把两者混在一起，会导致「越难的关弹药越多」这种自我抵消。

---

## 4. 四层方案

### ① 挑战变体 —— 半天，见效立刻

**动机**：把「10 个一次性关卡」变成「30+ 个可刷目标」。这是投入产出比最高的一改。

**1. 星级语义重构**（顺带修掉三个 bug）

| 星 | 现在 | 建议 |
|---|---|---|
| ⭐ | 完成目标 | 完成目标（不变） |
| ⭐⭐ | 剩弹 ≥ 2（所有关都一样） | 剩弹 ≥ `ceil(ammo × 0.4)`（与弹药挂钩） |
| ⭐⭐⭐ | `score ≥ targetScore` **且** 剩弹 ≥ 2 | **在 `ceil(设计E)` 发内完成目标** |

- 新的 3★ 定义**直接来自判据一**：L1/L3 的 3★ 就是「一球通关」（E ≤ 1），L9/L10 的 3★ 是「两发通关」。**星级门槛成了难度设计的自然产物，不再是手填的常数。**
- **`targetScore` 从三星判定里剥离**，只在结算界面作为「打得漂亮」的展示项。这一步同时消灭 L1 的三二星等价与 L4 的数学不可能。
- ⚠️ 需要多发改动后的复验：一发最好是在**固定初始棋盘**上扫出来的，连续多发时棋盘会变。3★ 的可达性必须用 `playtest.mjs` 加一段「多发改动序列」实测确认，不能只靠推算。

**2. `targetScore` 反推公式**（保留作展示分）

```
targetScore = round_to_50( 目标砖块分值 × 1.35 )
       其中 目标砖块分值 = 目标数 × 100（颜色关 = 目标块数 × 100）
```

`1.35` 是对连击奖励的期望值（现行公式 `bonus = 分值 × 0.25 × (连击数−1)`，一次 6~8 连的坍塌大约贡献 20~35%）。

**3. 成就徽章**（不计星，横向丰富度，与总进度一起展示）

| 徽章 | 判定 | 用意 |
|---|---|---|
| 不碰玻璃 | 全程 `glassShattered === 0` | 反常规打法 |
| 全场清空 | 可击倒砖全部击倒 | 追求极限 |
| 零连击 | `maxCombo ≤ 1` 完成 | 反直觉（要克制连锁） |
| 剩弹大师 | 剩弹 ≥ `ammo − 1` | 精准 |
| 不换弹药 | 未使用备用弹药完成 | 硬核 |

**4. 持久化**

```
localStorage key: "cb:progress:v1"
value: { stars: number[10], badges: string[] }
```

首页显示 `★ 14 / 30` 与徽章墙。实现路径与语言偏好（`src/i18n.ts`）完全一致，照抄即可。
⚠️ **不要**和语言的 key 合并 —— 语言要跟着「清了进度」也保留。

**5. 关卡解锁门槛 —— 决策 #1 取了 B（做门槛），与本文原建议相反**

本文原建议「保持开放」（分享型小游戏，锁关卡会挡住试玩）。阿光的取舍是**改成 B：完成前一关才解锁下一关**，理由是「重复感强、挑战不足」的病根之一就是「随便跳关、没有推进感」。落地时做了三点缓解：① 第 1 关永远可进，新玩家不会被挡在门外；② 锁住的按钮是 `disabled` + 虚线边框 + 🔒「未解锁」，**不是**「点了没反应」（后者会被读成坏了）；③ 挑战模式通过**也算通关**并解锁下一关，不会逼玩家打两遍。

> ⚠️ 这条**与分享场景有直接冲突**：把链接发给朋友，对方只能玩第 1 关。如果以后要拿它做分享 demo，需要加一个「解锁全部」的调试入口或者默认开放。

---

### ② 打法维度 —— 1~2 天

**动机**：现在只有一颗标准球 ⇒ 每关只有一条最优解。多弹药让同一关出现**多解与取舍**。

> ⚠️ **下表是 v1.0 的提案，不是最终数值。** 实际实现改了四处（铁锤初速、爆破筒的作用方式与引爆条件、三连发的单颗威力），**最终值见第 9.2 节**。`Game.ts` 里也没有 `addBall()` 这个函数名，实际是 `spawnBall(dir, def)`。

| 弹药 | 关键参数 | 克制 | 代价 |
|---|---|---|---|
| 标准球 | mass 9 / r 0.45（现状） | 通用 | —— |
| 铁锤 | mass 18 / r 0.62 / 初速 ×0.80 | 厚墙、静态平台边缘 | 弹道更低更弯，打不到高处目标物 |
| 爆破筒 | 命中后 0.30s、半径 3.4、冲量 900 | 玻璃、密集砖 | 会连带毁掉自己的落点与相邻结构。⚠️ **最终实现没要那个 0.30s 延迟**，见第 10 节 |
| 三连发 | 一次 3 颗、±7° 散开、各 60% 威力 | 宽面墙、**并行承重单元**（正是判据二要求的结构） | 精度差，打目标物几乎不可用 |

**关键设计点**：三连发与爆破筒**恰好对症判据二** —— 当 L3/L8/L10 被拆成多个独立承重单元后，「三连发同时打多个单元」成为唯一解，机制与结构互相成立。

**规则**：每关默认一种弹药 + 可换一种备用（不消耗额外次数，只是选择）。这样单关解空间从 1 变成 N，且**不需要新美术资产**（换色 + 换尺寸即可）。

**配平方法**：用 `playtest.mjs` 对每种弹药各扫一遍，输出「同关各弹药的 `一发最好`」矩阵 —— 差异就是这批弹药的收益表，配平不用猜。

**实现注意**（✅ 三项都已按这个思路落地，见第 9 节）：
- 原 `spawnBall()` 里质量/半径/初速是常量，已改成从弹药定义读（`AMMO[kind]`）。
- 爆破筒的「引爆 + 冲击波」走了 `world.step()` 之后的清算点 —— 与 `drainShatters()` 同一原则：**回调内只置标记，step 之后再动世界**。（触发方式在 v1.2 由「引信烧完」改成「命中当帧」，但这条帧序纪律没变，也不能变。）
- ⚠️ **改弹药参数必须同步 `tools/playtest.mjs` 顶部的 `AMMO` 表**。注意 `check-levels.mjs` **不开炮**，所以它**不需要**复刻弹药（这一点 v1.0 写错了，已在 README 与 HANDOVER 更正）。漏同步不报错，只是工具会静默按标准弹算出一个与真机不符的「一发最好」。

---

### ③ 约束机制 —— 每项半天，逐项引入

**动机**：`E` 反推出来的「两发」仍然是可背的。约束机制的作用是**让背下来的弹道失效**。

按引入顺序（复杂度递增，**每次只加一个**，加完必须重跑配平）：

| # | 机制 | 数值建议 | 打掉什么 | 数据字段 |
|---|---|---|---|---|
| 1 | **风** | 横向恒定加速度 ±3 ~ ±8 m/s²，HUD 风向箭头 | 「背弹道」——一次改动收益最大 | `LevelDef.wind` |
| 2 | **限时** | 20 / 15 / 12 秒，超时判负或扣一发 | 「算」→ 逼向「手感」 | `LevelDef.timeLimit` |
| 3 | **移动目标** | 目标砖沿 z 往复，幅度 4m、周期 2.4s | 提前量预判 | `BrickDef.motion` |
| 4 | **易爆砖** | `BrickKind = 'bomb'`，碎裂时对周围 `applyImpulse` | 制造「连锁拆解」的主动规划 | 砖种（见下） |
| 5 | **锚砖** | 抽掉整片崩的关键砖 | 「先打哪」的决策 | `BrickDef.anchor` |

**⚠️ 机制 1 与 2 不要同时引入**：风要求玩家重算弹道（需要时间），限时要求玩家不能算 —— 两者会互相抵消，玩家只会觉得「随机」。

**机制 4 的正确实现姿势**：`bomb` 砖**复用玻璃的管线** —— `queueShatter()` 入队 → `world.step()` 之后 `drainShatters()` 清算（回调在 step 内部，求解器还握着刚体）。爆炸冲量在清算点施加。这是对 `levels.ts` 顶部「加砖种要动的 5 处」清单的**第一次真实检验**，第 4 条（`tools/` 里两个物理复刻）最容易漏。

---

### ④ 无限内容 —— 2~3 天，天花板最高

**动机**：判据一/二决定了「手工加难」在 L3/L8/L10 会撞墙。真正的解药是**换内容而不是加难度**。

**「每日一关」生成器**

```
种子 = hash(日期)          → 同时作为排行榜的匹配键
   ↓
从构件库（wall / tower / arch + 砖种）按「零件预算」随机拼装
   ↓
校验闸门 A：tools/check-levels.mjs 的摆放静检
           （每块非静态砖底面必须有支撑面；悬空/埋入/自倒 ⇒ 丢弃重掷）
   ↓
校验闸门 B：tools/playtest.mjs 的弹道解算
           （无解 ⇒ 丢弃；有解 ⇒ 用判据一反算目标数，自动定难度）
   ↓
输出：当日关卡的 bricks / objective / ammo / 设计E
```

**这个方案的关键优势：你手上已经有两把别人要花两周才有的尺子。** 生成器最怕的是生成不可玩的东西，而你的 `check-levels.mjs` 恰好就是「摆砖合法性」的判定器、`playtest.mjs` 恰好就是「可解性与难度」的判定器。把它们串成闸门即可。

**配套**：
- **排行榜**：同种子比分数。现行分数公式（连击 + 剩弹）天然可排名，无需改动。
- **沙盒分享**：`BrickDef[]` 本身就是可序列化的纯数据，导出/导入布局近乎零成本。
- ⚠️ 生成器必须先有 ① 的持久化与展示打底，否则「每日一关」的好成绩第二天就清零，玩家不会来第二次。

---

## 5. 落地数值表（设计意图，**必须用 `playtest.mjs` 复算后定稿**）

> 下表的「新 E」是按判据一算出的意图值。**只有跑完 `playtest.mjs` 实测 `一发最好` 之后才能确认**，
> 因为改结构会改变 `一发最好` 本身（L6/L9 在拉近靶场时就出现过反向变化，见 README 难度表注释）。

| 关 | 形态 | 可击倒 | 一发最好 | 现目标 | **新目标** | 现 E → 新 E | 手段 |
|---|---|---|---|---|---|---|---|
| L1 | 数量 | 26 | 20 | 8 | **16** | 0.40 → 0.80 | 纯数值 |
| L2 | 目标物×1 | 18 | 19 | 1 | 1（下沉一层加护罩） | ≤1 → 2.0 | **结构改造** |
| L3 | 数量 | 22 | 21 | 14 | **20**（撞上限） | 0.67 → 0.95 | 纯数值（上限）；再难需拆结构 |
| L4 | 红砖×4 | 16 | 9 | 4 | 4 → **分散两处各 2** | 0.44 → 2.0 | **结构改造**（与②的爆破筒配合） |
| L5 | 数量 | 51 | 41 | 30 | **43** | 0.73 → 1.05 | 纯数值 |
| L6 | 数量 | 38 | 26 | 18 | **34** | 0.69 → 1.30 | 纯数值（近上限） |
| L7 | 目标物×2 | 36 | 14 | 2 | 2（**已合格，不动**） | 2.0 → 2.0 | —— |
| L8 | 数量 | 38 | 38 | 19 | 19 → **拆结构后重测** | 0.50 → 1.5 | **结构改造**（单发 100%，必须拆） |
| L9 | 数量 | 54 | 17 | 12 | **31** | 0.71 → 1.82 | 纯数值（余量最大） |
| L10 | 数量 | 65 | 47 | 28 | 28 → **拆结构后重测** | 0.60 → 2.0 | **结构改造**（单发 72%，超上限） |

**弹药建议**

| 关 | L1 | L2 | L3 | L4 | L5 | L6 | L7 | L8 | L9 | L10 |
|---|---|---|---|---|---|---|---|---|---|---|
| 现在 | 5 | 5 | 4 | 5 | 6 | 5 | 5 | 5 | 6 | 6 |
| 建议 | 5 | 5 | 5 | 5 | 6 | 6 | 5 | 5 | 6 | **7** |
| 试错预算 | 3 | 3 | 3 | 4 | 4 | 4 | 3 | 4 | 5 | 5 |

**改这块的连锁动作（漏一条就会「数据改了但界面不对」）**
1. 改 `levels.ts` 的 `objective.count` / `ammo` / `targetScore` / `twoStarAmmoLeft`
2. 跑 `node tools/check-levels.mjs` ⇒ 必须 **10/10**（摆放贴合 + 自倒 0 + 玻璃自碎 0）
3. 跑 `node tools/playtest.mjs` ⇒ 复算 `一发最好`，确认新 E 落在设计值 ±0.15 内
4. **同步 `tools/check-levels-live.mjs` 的 `EXPECT`**（开局进度字符串），跑 ⇒ **54/54**
5. 同步 `README.md` 的关卡表与难度曲线表（这两张表本身就是交付物）

> ⛔ **状态：本节整张表都没落地**（2026-09-24）。`levels.ts` 的 `objective.count` / `ammo` / `targetScore` / `twoStarAmmoLeft` 全部保持 v1.0 之前的原值。这一轮做的是第 ①②层里的「弹药」与「挑战模式 + 持久化 + 门槛」，**数值重配平（M1）没碰** —— 因为按判据二，L3 / L8 / L10 想变难只能拆结构，那是比调数字大得多的一次改动，值得单独一轮做。

---

## 6. 实施顺序与里程碑

| 里程碑 | 内容 | 工作量 | 验收 | 状态 |
|---|---|---|---|---|
| **M0** | 星级重构（3★ = `ceil(设计E)` 发内完成）+ `targetScore` 剥离三星判定 + 持久化 + 首页总进度 | 0.5 天 | 真机 CDP 回归：星级写入后刷新仍在；L1 的三二星不再等价 | 🟡 **部分**：持久化 + 首页/面板总进度已完成；**星级重构按决策 #3 改成「挑战模式另起一套判定」，普通模式星级未动** |
| **M1** | `targetScore` 反推 + 目标数重配平 + L2/L4 结构改造 | 0.5~1 天 | `check-levels` 10/10；`playtest` 复算 E 落在设计带；`check-levels-live` 54/54 | ⛔ **未做**（第 5 节的数值表没落地，`levels.ts` 的 `objective.count` / `ammo` / `targetScore` 全部保持原值） |
| **M2** | 四种弹药 + 弹药选择 UI | 1~2 天 | 各弹药 `一发最好` 矩阵输出成表 | 🟢 **已完成**（2026-09-24）：`ammo.ts` + 右下角切换按钮 + 暂停面板弹药图鉴。**实际参数与本文提案不同，见第 9.2 节**；爆破筒的引爆方式当天又改了一版（引信 → 接触引爆，见第 10 节） |
| **M3** | 风 → 限时 → 移动目标 → 易爆砖（**逐项**，每项独立可上） | 每项 0.5 天 | 每加一项重跑 `check-levels` + `playtest` | ⛔ 未做（决策：先不做） |
| **M4** | 每日一关生成器 + 排行榜 | 2~3 天 | 连续 30 天种子全部通过双闸门；无不可解/自倒关卡 | ⛔ 未做；决策 #4 取 B（生成器同时兼顾数量 / 目标物 / 颜色目标），#5 为「先不做后端」 |
| **M0.5** | （v1.1 追加）**挑战模式**：1 发定胜负，独立判定与独立记录 | 0.5 天 | 真机回归：徽标 / 1 发弹药 / 一发结算 / L4 走积分线 550 | 🟢 **已完成**（2026-09-24）。它是对决策 #3 的另一种答法：**不开刀普通模式星级，另起一套** |

**M0 单独可交付** —— 即使后面全不做，星级重构 + 持久化这一项就能把内容量放大三倍。

> 实际这一轮交付的是 **M0 的持久化部分 + M2 + 新增的 M0.5（挑战模式）**。也就是说「让进度可积累」这一条做到了，「让每关有多个可追求目标」靠的不是星级重构而是**挑战模式**（每关多了「一发通关」这个独立目标，L4/L7 是积分线）。

---

## 7. 反模式（写在这里免得下次踩）

1. **只加目标数、不加机制**。把 L5 的 30 改成 43 而不给任何新手段，结果是「同一件事做十遍，更累」——重复感会更重，不是更轻。E 从 0.4 提到 0.8 是「教学 → 正戏」，**从 1.3 往上必须有新机制补偿**。
2. **无视判据二去调数字**。L3/L8/L10 的单发覆盖率分别 95% / 100% / 72%，调数字会在 `可击倒 × 0.95` 上限撞墙，只会得到「要求玩家全清」的挫败感。
3. **星级做成「三个递进条件」**。原设计就是这个错：3★ 的达成以 2★ 为前提，于是「三星」不再是一个独立目标，而是「二星的严格版」。必须是三个各自可达成的挑战。
4. **星级门槛由剩弹奖励抬着**。`score` 含 `ammo × 200` 时，任何「多留弹药」的星都会自动把分数星一起满足。
5. **一次加多个机制**。加了风和限时之后如果手感变坏，无法归因是哪一个。**一次一个，加完重跑配平。**
6. **把 `一发最好` 当成玩家平均水平**。它是求解器扫出来的上限。玩家实际单发战果大约是它的 40~60%，所以「弹药 = 试错预算」永远要留够。
7. **改结构而不重测 `一发最好`**。拉近靶场（`FIELD_SHIFT_Z` 0 → 6）时，L6 单发从 19 涨到 26、L9 从 28 掉到 17 —— **同一次改动对两关方向相反**。任何结构/景别改动都必须重跑 `playtest.mjs`。

---

## 8. 决策记录（2026-09-24 拍板）

| # | 议题 | 选项 | **决定** | 落地状态 |
|---|---|---|---|---|
| 1 | 关卡解锁门槛 | A 保持全开／B 完成前一关解锁下一关 | **B** | 🟢 已实现（`progress.ts` 的 `isUnlocked`）。⚠️ 与分享场景冲突，见第 4 节 ① 第 5 条 |
| 2 | 弹药选择方式 | A 每关预选／B 关内切换 | **B** | 🟢 已实现（右下角按钮循环 + 暂停面板图鉴）。**弹药跨关卡保留**，切弹药不消耗次数 |
| 3 | 3★ 判定基准 | A `ceil(设计E)` 发内完成／B 固定「一球通关」 | **「可按挑战模式单独走这个方式试试」** | 🟢 已实现为 **M0.5 挑战模式**（另起一套判定与记录）。**普通模式的星级判定一行没动** —— 所以根因三那三条仍在 |
| 4 | 生成器的目标形态 | A 只生成数量目标／B 也生成目标物与颜色目标 | **B** | ⛔ 未做（生成器本身是 M4，未开工）。方向已定：生成器要兼顾数量 / 目标物 / 颜色三类目标 |
| 5 | 排行榜是否需要服务端 | A 本地记录／B 接云服务 | **先不做** | ⛔ 未做。若以后要做，最小形态是**本地**记录每关挑战的最好得分，不上服务器 |

> 决策 3 的读法值得记一笔：本文原本把它设成「改普通模式的星级判定」，阿光选的是**别动那个系统，另开一套**。这个取舍的结果是「挑战模式」成了一个**独立的玩法模式**而不是一次数值调整 —— 好处是不破坏现有手感与已通过关卡的记录，代价是两套判定要各自维护（见 `HANDOVER.md` 陷阱十二的「记录完全分开写」纪律）。

---

## 9. 本轮实际落地（v1.1，2026-09-24）

### 9.1 挑战模式（M0.5）—— 决策 3 的落地形态

**规则**：教程浮层与暂停面板各一个开关；开启后**每关 1 发、一发定胜负**，失败即重来。HUD 出紫色徽标、弹药指示变 1 颗。

**判定基准**由关卡数据给出（`LevelDef.challenge?: ChallengeRule`），而不是散在引擎里：

| `judge` | 判据 | 用在哪 | 理由 |
|---|---|---|---|
| `'complete'` | 一发就达成关卡目标（复用 `objectiveDone()`） | 默认（关卡不写 `challenge`） | 这些关存在「一发即胜」的弹道（`E ≤ 1`） |
| `'score'` | 一发得分 ≥ `scoreLine`，**或**一发直接完成了目标 | L4（550）· L7（1800） | 这两关的目标**形态上一发不可能完成** |

**为什么 L4 / L7 必须走积分**（这条是判据一的一次实战检验）：

- **L4** 目标「砸掉 4 块红砖」—— 四块全堆在挡板后的高台上（`x = 5.5 ~ 8.5`、`z = −30`），一发最多只吃得到一侧
- **L7** 目标「砸掉 2 颗紫色目标」—— 两颗分列 `x = −7` 与 `x = +7`，一发在物理上不可能同时命中（真 `E = 2`，正是第 1.2 节说的**度量盲区**）

**`scoreLine` 的取值法**（**2026-09-24 修订**）：跑 `tools/playtest.mjs` 拿到**单发最高分**，取它的约 **50%**，再取整到 50。

旧公式 `一发最好 × 100 × 0.6` **只数砖块、漏算连击奖励**：分子 `100` 是单块砖基础分（见 README「计分」），但连击会在 1.6 秒窗口内把「每多倒一块」的收益再抬高 25%，所以真实得分能比估算高 3~4 倍。实测（2026-09-24，`playtest.mjs` 补上单发得分输出后）：

| 关 | 一发最好 | 单发最高分 | 旧 `scoreLine` | 占最高分 | 现 `scoreLine` | 占最高分 |
|---|---|---|---|---|---|---|
| L4 | 9 块 | 1800 | 550 | 31% | 550（未动） | 31% |
| L7 | 14 块 | **3675** | 850 | **23%** | **1800** | 49% |

L7 的 850 等于「随便打打就过」—— 这就是「得分类关卡难度过低」的来源。1800 相当于要求打掉 9 块且全部连击（`100×9 + 25×36 = 1800`），是单发最高分的 49%、一发最好的 64%。

> ⚠️ **加关卡 / 改目标时的自查**：「这个目标能不能用一发同时达成？」不能，就必须写 `judge: 'score'` 并给一条 `scoreLine`，否则这关在挑战模式里**永远通不过** —— 不是难，是不可能。这句自查同时抄在 `levels.ts` 的 `ChallengeRule` 注释上。

**双记录纪律**：挑战通过走 `markChallengePassed()` + `markCleared()`（也解锁下一关），**不写普通模式的星级**；`finish()` 的挑战分支直接把 `stars` 置 0、也不加弹药奖励（只有 1 发，`ammo × 200` 那个算式没有意义）。

### 9.2 四种弹药（M2）—— 最终数值

⚠️ **这张表才是权威**，第 ② 节那张是 v1.0 的提案（参数与实现不同）。

| 弹药 | 半径 | 质量 | 初速倍率 | 一次发射 | 颜色 | 实现要点 |
|---|---|---|---|---|---|---|
| `standard` 标准弹 | 0.45 | 9 | ×1.0 | 1 | `ENV.ball` | 历史所有难度数字的基准，**不要动** |
| `hammer` 铁锤弹 | 0.62 | 18 | ×0.88 | 1 | `ENV.metal` | 质量翻倍撞得动厚墙；射程只剩约 77% |
| `bomb` 爆破筒 | 0.5 | 11 | ×1.0 | 1 | `PALETTE.red` | **接触引爆**（命中当帧，无引信延迟），半径 3.4m，中心 `Δv = 14` |
| `triple` 三连发 | 0.32 | 5 | ×1.0 | 3（±7°） | `PALETTE.purple` | 单颗动能约为标准弹的 55%，胜在能同时够到多个独立承重单元 |

**与 v1.0 提案的四处偏差**（都是实现时改的，记下来免得下次照提案改回去）：

| 项 | v1.0 提案 | 实际实现 | 为什么改 |
|---|---|---|---|
| 铁锤初速 | ×0.80 | **×0.88** | 0.80 太钝 —— L1 上 22 个候选弹道只有 2 个能命中，工具扫出来一片 0，玩家体验是「打不着」而不是「更沉」 |
| 爆破筒作用方式 | 「冲量 900」 | **按速度增量 `Δv`（中心 14，边缘线性衰减到 0）再乘该砖 `mass`** | 写死冲量时实际效果是 `impulse / mass`，砖块质量 1.0~1.8 不等 ⇒ 轻砖飞天、重砖不动 |
| 爆破筒引爆条件 | （未提） | **第一次有效命中（法向速度 > 1.5）当帧引爆** | 阈值用来放过纯擦碰，避免出膛蹭到什么都炸。⚠️ v1.1 落地时这里还有一层 0.3s 引信，2026-09-24 实测后**去掉**（见第 10 节） |
| 三连发单颗威力 | 「各 60% 威力」 | **质量 5（约 55%）+ 半径 0.32** | 质量与半径都要降，只降质量的话单颗反而更「穿」 |

**配平方法已落地**：`node tools/playtest.mjs <关号> --ammo <kind>` 可以对每种弹药各扫一遍，输出「同关各弹药的 `一发最好`」。

### 9.3 交叉验证：弹药真的进了物理（不留「参数没生效」的怀疑）

在 **L1**（普通砖墙，26 块可击倒）上换弹药扫，**击倒分布的形状完全不同**：

| 弹药 | 22 个候选弹道的击倒分布 | 一发最好 |
|---|---|---|
| `standard` | 0×4 · 4×2 · 5 · 6×3 · 7 · 8 · 9×3 · 10×4 · 11 · 12 · **20×1** | 20 |
| `hammer` | **0×20** · 15×1 · **20×1** | 20 |
| `bomb` | （分布不同，最大值更高） | **24** |
| `triple` | 0×3 · 1×2 · 3×3 · 4×2 · 5 · 6 · 7×2 · 10 · 11×2 · 13 · 14 · 17 · **20×2** | 20 |

- `hammer` 的 22 个候选里只有 **2 个**能命中 —— 初速 ×0.88 让大部分按标准弹反解出来的弹道打不到，这是 `powerScale` 生效的直接证据。
- `bomb` 的最大值是 **24**，高于标准弹的 20 —— 爆炸的额外收益可见。
- **三种弹药的「一发最好」恰好都是 20**，这是 L1「高抛一发推倒整面墙」在 20 块处**饱和**的巧合（同一个候选 yaw 2.3° / pitch 68° / power 22.3 对三种弹药都是最优），**不是参数没生效**。要看差异得看分布形状，或者看 `bomb` 的 24。

> ⚠️ 顺带发现一个**工具的盲区**：那个最优解用的是 **pitch 68°**，而鼠标瞄准的映射上限只有 **65°**（`pitch = -ny·90 + 20`，`ny ∈ [-0.5, 0.5]`）—— 也就是说这条弹道玩家用鼠标点不出来，只有按 `W`/`S` 才够得到。**在改掉 `playtest.mjs` 的 `PITCHES` 之前，别拿 pitch 68 那一档的数字去定关卡难度。**

---

## 10. 爆破筒改为接触引爆（v1.2，2026-09-24）

### 10.1 起因：一句话点破了 v1.1 引信的隐藏假设

阿光的反馈是：**「命中后 0.3 秒爆炸几乎没有效果，我们现在的关卡设计缺少纵深，掠过平台基本就没有用了」**。

`BOMB_FUSE = 0.3` 这版引信藏着一个假设：**目标是「有厚度、能把球吃掉」的结构**。0.3 秒在时速 40 米下等于 **12 米**飞行距离 —— 只有当球在这段时间里被结构拦住（撞进厚墙、被弹回来），爆炸才会落在结构内部；一旦目标是「薄壳 + 一块平台」（现在 10 关里绝大多数），球从外壳上擦过去根本不停，12 米之后早就把整个关卡甩在身后了。玩家看到的就是「炸了个寂寞」。

### 10.2 改法：把「时长」换成「阈值」

| | v1.1（旧） | v1.2（新） |
|---|---|---|
| 常量 | `BOMB_FUSE = 0.3`（**时长**） | `BOMB_TRIGGER_V = 1.5`（**触发阈值**） |
| 触发 | 第一次有效命中（法向 > 1.5）开始**计时** | 第一次有效命中（法向 > 1.5）**当帧引爆** |
| 兑现时机 | 引信烧完那一帧（命中后约 18 帧 ≈ 0.3s） | **命中那一帧** |
| 爆炸中心 | 取决于球在 0.3s 内飞到哪 —— 厚墙内部 / 半空中 | **永远是接触点** |
| `Ball` 字段 | `armed` + `fuse`（两个） | `detonate`（一个布尔标记） |

阈值**保留**，但它的角色变了：不再是「点火条件」，而是**纯擦碰过滤器**。cannon-es 对「贴着平台滑一下」「被别的球轻推一下」也会发 `collide`，没有阈值的话球出膛蹭到什么都炸。

> ⚠️ **帧序没有变，也绝对不能变**：`collide` 回调里**只置一个纯数据标记**，真正的 `explode()` 仍在 `world.step()` 之后（见 `HANDOVER.md` 陷阱十一坑 C）。「用什么触发」与「在哪兑现」是两件互不相干的事 —— 后者只由帧序决定。

### 10.3 前后实测对比（`tools/playtest.mjs --ammo bomb`，全 10 关）

| 关 | 可击倒 | 目标 | **旧：0.3s 引信** | **新：接触引爆** | 变化 | 标准弹（未改动的基线） |
|---|---|---|---|---|---|---|
| L1 | 26 | 8 | 24 | **15** | **−9（−38%）** | 20 |
| L2 | 18 | 1 | 18 | 18 | 0 | 18 |
| L3 | 22 | 14 | 19 | **22** | **+3（+16%）** | 21 |
| L4 | 16 | 4 | 10 | 10 | 0 | 9 |
| L5 | 51 | 30 | 32 | **38** | **+6（+19%）** | 41 |
| L6 | 38 | 18 | 25 | 26 | +1 | 26 |
| L7 | 36 | 2 | 14 | 14 | 0 | 14 |
| L8 | 38 | 19 | 38 | 38 | 0 | 38 |
| L9 | 54 | 12 | 24 | 22 | −2（−8%） | 17 |
| L10 | 65 | 28 | 54 | 52 | −2（−4%） | 47 |
| **均值** | | | **25.8** | **25.5** | **−1%** | 25.9 |

**读数**（两列都来自 `--ammo bomb`，均值只差 1% —— 这次改动的效果不是「变强/变弱」，而是**分布被改写**）：

- **L1 是唯一的显著下降（24 → 15）**：L1 也是**唯一一个有「厚度」的关**（26 块砖垒成一面实心墙）。引信版本让球有时间钻进墙体，爆炸发生在**结构内部** ⇒ 收益最大；接触引爆把爆炸中心移到**表面** ⇒ 收益掉回 15（比标准弹的 20 还低一点）。
- **L3（+16%）与 L5（+19%）是显著上升**：这两关是薄壳/散件结构，旧引信下球常常在结构**外面**点火，白飞 12 米。改成接触引爆后，爆炸落在了它真正该炸的地方。
- **L9 / L10 各掉 2 块**：可击倒数最大（54 / 65）的两关，深部砖本来还能被引信版波及到一点，现在够不着了。
- **L2 / L8 两关都吃满**（18/18、38/38 = 100%）—— 爆破筒的「玻璃克星」定位没有丢。

### 10.4 结论

**这不是削弱，而是把炸弹还给它的本职。** 引信版本在 L1 那种「有纵深的实心墙」上是更强的，但那样结构的关卡现在只有一关；在其余九关里，引信换来的是「球在结构外面点火、白白飞走」—— 正是阿光描述的手感。代价是爆破筒的身份从「钻进去从内部掏」变成「贴在表面炸」，所以它现在的定位是**玻璃 + 散件结构**的专用答案，而不是「打厚墙的首选」。

> 🔴 **反模式（写在这里免得下次踩）**：不要因为「L1 掉到 15 了」就把引信加回来。真正的缺陷在**关卡缺纵深**（现在 10 关里 9 关是薄壳），那是 M1 / M3 的活（给关卡加厚度、加前后错层），不是靠给武器塞一个延迟来掩盖的。

---

## 11. 爆炸表现层（v1.3，2026-09-24）

### 11.1 为什么这一轮**没有数值结论**

起因是一句纯观感的诉求：「爆破弹能否增加更明显的爆炸效果」。这一轮改动**一个数值都没动** —— 爆炸半径 `3.4m`、中心 `ΔV = 14`、边缘线性衰减、`BOMB_TRIGGER_V = 1.5`、四种弹药表全部保持第 9 / 10 节的样子。

所以本文不需要新增任何实测表。**但这一轮有一条纪律必须写进数值文档**，因为它关系到「改观感会不会悄悄改掉难度」：

> 🔴 **`explode()` 里那条分界线是数值与表现的边界。**
> `// ---------------- 以下是纯表现，一行都不影响上面算出来的物理 ----------------`
> 上面是 `EXPLOSION_RADIUS` / `EXPLOSION_DV` / 质量系数 / 玻璃判碎 —— 改任何一项都要重跑 `node tools/playtest.mjs --ammo bomb`（全 10 关）；
> 下面是闪光灯脉冲、`spawnBlastFx()`、三批 `emit()`、震屏、音效 —— 随便改，不影响难度。
> 本轮所有改动都落在分界线**下方**，所以**没有重跑 playtest**，`E` 值与第 1.2 / 10.3 节的表全部仍然有效。

### 11.2 震屏是唯一的「可能被误当数值改动」的项

`shake` 从 `+0.55` 提到 `+0.8`（上限 `0.95` 不变）。它确实会**短暂影响玩家读结果的能力** —— 而这在这个游戏里就是难度的一部分。

判断它没有越界的依据是**上限没动**：`0.95` 的上限决定了最坏情况下的画面位移，提高的只是「单次爆炸撞到上限的速度」。一次响指式的强震比持续抖动更容易被眼睛接受，因为**结算是在炮弹静止之后才发生的**，而那时震动早已衰减到 0。再往上调之前，先读 `explode()` 里那句「这是『重量』最便宜的来源，但**不能更大了**」的注释 —— 那条限制的理由是「会让人在读结果的那一刻看不清落点」。

### 11.3 与第 3 节判据的关系

无冲突。三条判据（目标数由 `E` 反推 / `E > 1.3` 需单发覆盖率 ≤ 65% / 弹药是试错预算）全部作用在「结构 + 目标 + 弹药数」上，与表现层正交。

⚠️ 但要留意一个**间接**影响：爆炸现在**更容易被看见**了，玩家会更快意识到「我这一发炸在哪」。这会让爆破筒的实际体感收益上升 —— 不是数值变强，是**反馈变清晰**了。第 10.4 节把爆破筒定位成「玻璃 + 散件结构的专用答案」，这个定位因为反馈变清晰而**更容易被玩家自己发现**，属于好事，不需要配平。

---

## 12. 变更记录

| 版本 | 日期 | 变更 | 作者 |
|---|---|---|---|
| v1.0 | 2026-09-24 | 首版。诊断（E 度量 + 五特性照检）、三条判据、四层方案、落地数值表、里程碑、反模式 | AI |
| v1.1 | 2026-09-24 | 5 条决策拍板（第 8 节）；补第 9 节「本轮实际落地」：挑战模式（M0.5）+ 四种弹药的最终数值与提案偏差 + 弹药生效的交叉验证证据 + pitch 68° 的工具盲区。里程碑表加状态列（M0 部分完成 / M2 完成 / M1·M3·M4 未做） | AI |
| v1.2 | 2026-09-24 | 新增第 10 节：爆破筒 `BOMB_FUSE`（0.3s 引信）→ `BOMB_TRIGGER_V`（接触引爆），附全 10 关前后实测对比（均值 25.8 → 25.5，但 L1 −38% / L3 +16% / L5 +19%）与「反模式：别把引信加回来」 | AI |
| v1.3 | 2026-09-24 | 新增第 11 节「爆炸表现层」。**本轮零数值变化**（半径 / ΔV / 衰减 / 触发阈值 / 四张弹药表全未动），所以不新增实测表、**没有重跑 `playtest.mjs`**，第 1.2 / 10.3 节的 `E` 表全部仍然有效。记下的实质内容只有三条：① `explode()` 里那条「表现段 / 物理段分界线」是数值与表现的边界，碰上面必须重跑难度工具、碰下面随便改；② 震屏 `0.55 → 0.8` 是唯一可能被误当数值改动的项，依据是**上限 0.95 未动**；③ 反馈变清晰会让爆破筒的**体感**收益上升（不是数值变强），第 10.4 节的定位不需要配平 | AI |
| v1.4 | 2026-09-24 | **L7 挑战积分线 850 → 1800**：旧公式「一发最好 × 100 × 0.6」漏算连击奖励，实测该发实际得 3675 分、旧线只相当于 23% 发挥（等于没难度）。新增取值法「单发最高分 × 约 50%」；`tools/playtest.mjs` 补上**单发得分**输出（逐帧比对击倒增量，复刻 `checkKnocked` 的 1.6s 连击规则），L4 的 550 未动。另：界面文案精简 + 改名，见 README | AI |
