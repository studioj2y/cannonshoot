# 视觉方向单 · Color Brick Cannon

> 主模式：`art-direction` ｜ 支持模块：`threejs-3d`（已确认 Three.js 项目，不走 Cocos）
> 版本 v1.3 · 2026-09-23 · 状态：**已执行并验收**（阿光 2026-09-23「以精致的 Low-Poly 风格展现」）
> v1.1 回填客观实测数据，v1.2 补扩关验收，**v1.3 新增玻璃砖材质族并重测渲染预算**；验收记录见第 11 节。
> 本文是「精致 Low-Poly」重构的规则源。改美术前先读这里；改完在文末「验收」表里补证据。

## 1. 目标

| 项 | 内容 |
|---|---|
| 业务目标 | 让手机端首次打开就产生「这是一款做完的游戏」的第一印象，降低首屏跳出 |
| 玩家行为目标 | 3 秒内看懂「目标在哪、炮口朝哪、下一炮会落到哪」 |
| 成功指标 | 首屏主次层级清晰可辨；砖块/目标在小屏上第一眼可辨；无霓虹色与视觉噪声 |
| 情绪 | 手工纸模微缩靶场 —— 安静、哑光、可信赖，而不是刺眼的玩具霓虹 |

## 2. 硬约束（不可违反）

1. **物理与输入一行不改**：`BrickDef` 的 `pos/size/color/mass/static` 全部保持，`CANNON.Box/Cylinder` 形状不变，`muzzle.position.z`、`cannon.position`、`aimDir`、`HudState` 字段不变。美术只改「看得见的那层网格」。
2. **零外部素材**：不引入图片/模型/字体文件。所有纹理用 canvas 程序化生成，所有几何用 three 内置体组合（构建产物必须仍是单个自包含 HTML）。
3. **移动优先性能**：1080p 级手机上 60fps 为目标，降级路径必须存在（见第 8 节）。
4. **调色板受限**：全场景色值只能取自第 5 节表格，禁止新增/临时手写十六进制色（粒子、彩带、UI 同源）。
5. **可读性优先于风格**：性能与风格冲突时保可读性；目标色（红）必须比其它砖色更饱和。

## 3. 方向陈述

> **像一套精心裁切的哑光纸板微缩模型，摆在有晨雾的山谷草坪上。**

三个候选方向的取舍（用同一维度评价）：

| 候选 | 轮廓 | 明度 | 色彩 | 材质 | 空间 | 淘汰原因 |
|---|---|---|---|---|---|---|
| A 高饱和玩具（现状） | 基本体、无剪影特征 | 全亮，无层次 | 6 色霓虹循环 | Lambert/Phong/Basic 混用 | 平地 + 球山，无纵深 | ❌ 视觉噪声，砖墙失去结构可读性 |
| B 卡通描边（Toon + 轮廓线） | 黑描边强 | 硬二分 | 可控制 | 需后处理 | 依赖后处理 | ❌ 描边与移动端性能冲突，且与哑光纸模气质不符 |
| **C 精致 Low-Poly（选定）** | 低面多面体、倒角 | 三段式明度 | 受限暖调 | 单一 flat-shaded 家族 | 远中近三层 | ✅ 零后处理、零素材、可读性最好 |

## 4. 可观察的视觉原则

非作者也能据此判断一张新画面是否属于本方向：

1. **受限调色板 + 明度分层**：全场景色值只有两处来源 —— **7 色砖色组 `PALETTE`** 与 **环境色表 `ENV`**（见 5.1 / 5.2），没有第三处。砖色为中低饱和、高明度；**红是唯一高饱和色**（它是 L4 的目标色，必须可辨）。玻璃砖不新增色位，由所属砖色朝 `ENV.glass` 混合得到（见 5.3）。
2. **一切 flat shading、低面数**：所有几何在 4–8 面量级（六棱柱 / 二十面体 / 四棱锥）。**禁止用球体做云和山**——球体在低面数下没有剪影特征，正是现状「爆米花云」的来源。
3. **倒角与勾缝**：砖块是带 1 段倒角的挤出体，相邻砖之间自然形成暗色勾缝线；砖面不允许是纯直角塑料板。
4. **地平线三段式**：远景山脉（受雾化淡出、偏冷）→ 中景树丛与岩石 → 近景斑块草地。**远景必须比近景更淡更冷**（大气透视），这是「精致」与「空旷」的分界。
5. **统一光照**：暖主光（投影）+ 冷补光（无投影）+ 半球光（草地反弹）。同一光源方向贯穿 UI 的高光与投影方向。
6. **目标物必须是最高明度**：宝石化的目标物 + 落点标记，保证小屏第一眼可辨。

## 5. 稳定规则（后续迭代不得漂移）

### 5.1 砖色组（`PALETTE`，`src/game/art.ts`）

| key | 色值 | 语义 | 备注 |
|---|---|---|---|
| `red` | `#D9544D` | 陶土红 | **唯一高饱和**，L4 目标色，亮度抖动 ±6% 上限 |
| `orange` | `#E08A45` | 暖琥珀 | |
| `yellow` | `#E7C169` | 沙黄 | 顶梁与桥面用 |
| `green` | `#7FA96A` | 橄榄鼠尾草 | 与草地同族，靠明度区分 |
| `blue` | `#5D8FA8` | 灰青 | |
| `purple` | `#8E6E9E` | 灰梅 | 目标柱体用 |
| `gray` | `#C9C0B2` | 暖石灰 | 全部 `static` 平台/底座 |

### 5.2 环境色

| 用途 | 色值 |
|---|---|
| 天空顶 / 中 / 地平线 | `#4E9BD4` / `#A9D3EA` / `#E8E3D3` |
| 太阳光晕 | `#FFF1D9`（复用主光色；**没有独立日盘**，别按「有日盘」去验收） |
| 草地 / 沙地 / 干草 | `#7BB25E` / `#DCC189` / `#B9A06B` |
| 远景山（近带 / 远带） | `#8FB6A0` / `#B9CFD6` |
| 雾 | **= `skyHorizon`**。代码里直接引用 `ENV.skyHorizon`，**不允许单独定义** —— 两者不同值会在地平线留下一道可见接缝 |
| 主光 / 补光 / 半球天光 / 半球地反 | `#FFF1D9` / `#B9D6FF` / `#DFF1FF` / `#9CB86E` |
| 云（上 / 下） | `#FDFBF4` / `#EAF0F3` |
| 炮弹 / 宝石 / 尾迹 / 闪光 / 落点标记 | `#40444F` / `#CFA9DD` / `#FFF0C4` / `#FFF3C4` / `#FFF6DC` |
| 岩石 / 树叶深·浅 / 黄铜 / 木 | `#9A938A` / `#6B9058`·`#8FB573` / `#C9A24D` / `#6B5346` |
| **玻璃砖的混合基准色** | `#D6F0F5`（`ENV.glass`）—— 见 5.3 |

### 5.3 玻璃砖（`BrickKind = 'glass'`）

玻璃砖**不占新的砖色位**，也不在 `PALETTE` 里加值。它的颜色规则是「**所属砖色朝 `ENV.glass` 混 75%**」：

```ts
const c = new THREE.Color(brickColor);   // 该砖在 PALETTE 里的色值
if (variant === 1) c.multiplyScalar(1.06); else if (variant === 2) c.multiplyScalar(0.94);
c.lerp(new THREE.Color(ENV.glass), 0.75);
```

这么做的理由：**玻璃要让人一眼看出「它和旁边那块同色实心砖是同一族，但材质不同」**。直接换成纯玻璃青，蓝色的玻璃柱就和蓝色实心柱失去血缘关系；只加透明度不换色，在小屏上又和实心砖分不开。混 75% 的结果是「淡青蓝」——既保留色相归属，又明显比实心砖亮、比实心砖浅。

材质规格（`glassMaterial(color, variant)`）：

| 项 | 值 | 理由 |
|---|---|---|
| 家族 | `MeshPhongMaterial{ flatShading: true }` | 与全场景同一家族，不引入新 shader 家族 |
| `transparent` / `opacity` | `true` / **`0.6`** | 低于 0.5 就看不清砖形了；0.6 是「能透、但轮廓仍在」的下限 |
| `depthWrite` | 保持默认 `true` | 玻璃是**实体**不是薄片，关掉会让后面的砖穿透出来 |
| `shininess` | **`130`** | ⚠️ **全场景唯一的例外** —— 其它材质一律 `shininess ≤ 20`。高光是「玻璃」与「哑光砖」之间最主要的视觉差别，靠它才能在低多边形下读出材质 |
| `specular` | `#FFFFFF` | 配合高 shininess 才有明确的高光点 |
| 投影 | **`mesh.castShadow = false`** | 半透明物投影会在低多边形场景里显脏；关掉后碎掉的瞬间不会有「影子还在」的错觉 |
| 碎屑 | `shardGeometry()` = 三棱柱 `0.055 / 0.15 / 0.32`，颜色恒为 `ENV.glass` | 比普通粒子（`icoGeometry(0.12,0)` 二十面体）更细更长，读起来像碴子而不是石屑 |

**加新砖种时照这套走**：颜色规则写在 `art.ts` 的材质工厂里（不在 `Game.ts`），并且必须复用 `matCache`（键格式 `'glass|<色值>|<variant>'`）。

> ⚠️ 玻璃的碎屑几何体 `shardGeometry()` **故意不走 `cachedGeo`** —— 它比照炮弹粒子：每颗粒子的 `fadeMaterial()` 是独占的、要逐颗 dispose，几何体跟着一起走才对称。这是第 5.4 节禁例里「共享 vs 独占」的第二个例外，**不要「顺手优化」成缓存**。

### 5.4 禁例

- ❌ 新增球体云、球体山（云走 `cloudGeometry()`，山走 `mountainGeometry()`）。
- ❌ 在 `Game.ts` 里直接写 `new THREE.MeshPhongMaterial({...})` 配临时色值 —— 一律走 `art.ts` 的 `mat()` 缓存工厂。
- ❌ 让砖块视觉尺寸偏离碰撞尺寸超过 6%（倒角内缩上限 0.05）。
- ❌ 把目标物做成和普通砖同明度的形态（目标物必须是宝石造型 + 高明度）。
- ❌ **把 `art.ts` 的共享几何体 / 材质当成「某个对象的私有资源」去 dispose**。`mat()` / `brickGeometry()` 等全部带缓存，同色同尺寸的砖共享同一份；在 `removeBall()` / `reset()` 里 dispose 会把其它对象一起弄坏（症状是整场物体消失，且不报错）。共享资源只在 `disposeArt()` 收口 —— **例外只有三处**：每颗粒子独占的材质（`fadeMaterial()`）、每颗炮弹独占的尾迹几何体、每片玻璃碎屑独占的 `shardGeometry()`。
- ❌ 用 `mat()` / `unlit()` 去做需要「逐实例改 opacity」的东西（粒子、彩带、炮口闪光）。它们必须走 `fadeMaterial()` / `flashMaterial()` 这类**独占材质**工厂，否则同色粒子会整批一起淡出。
- ❌ **给玻璃砖写临时色值 / 自己 new 一个透明材质**。玻璃一律走 `glassMaterial()`（颜色 = 砖色朝 `ENV.glass` 混 75%，见 5.3），色值仍然只有 `PALETTE` + `ENV` 一处。
- ❌ **把玻璃做成「看起来透明但仍在投影」**。玻璃砖必须 `castShadow = false`（5.3 表里已列），否则碎掉后影子还在，会被读成渲染错误。

## 6. 生产规格

### 6.1 目标画幅与构图

| 项 | 规格 | 证据状态 |
|---|---|---|
| 设计基准 | 横屏 1440×900（桌面）与 412×915 竖屏（手机）双基准 | 已验证 |
| 竖屏安全区 | 顶部两端卡片不进画面中央 40%×30% 区域；底部控件高度 ≤ 22vh | 已验证（截图） |
| 相机 | 距炮台 5.5 水平后退 + 离地 3.4，FOV 60 | 已验证（射线检测遮挡 0%） |
| 焦点区 | 画面中央 60% 宽 × 45% 高 —— 砖墙/塔必须完整落在此区 | 已验证（老 5 关目标全部在画面内；新增 5 关的入场截图见 11.4） |

### 6.2 视觉层级（由强到弱）

1. 砖墙 / 塔 / 目标宝石（最高明度 + 最强投影）
2. 轨迹虚线与落点标记（次高明度，半透）
3. 炮台（暖色但只露出画面下缘 ~20%）
4. 环境（草地斑块 / 树丛 / 岩石，饱和度最低）
5. 天空与远景山脉（最淡，被雾化吸收）

### 6.3 首屏与资源预算

| 项 | 目标 | 实测（L1 / **L10 最重** / 手机 L1 降级档） |
|---|---|---|
| 首屏包体 | 单个自包含 HTML，≤ 950 KB / gzip ≤ 265 KB | **927.54 KB / gzip 254.07 KB** ✅ |
| 关键视觉资源上限 | `0` 个外部文件（纹理全部 canvas 生成） | ✅ 0 |
| 三角形总数 | ≤ 120 K（低端档 ≤ 80 K） | **9.7 K / 11.7 K / 6.4 K** ✅ |
| 绘制批次（含阴影趟） | ≤ 260（低端档 ≤ 180） | **82 / 153 / 82** ✅ |
| 其中：环境 + 轨迹线 + 落点标记 | ≤ 14 | **12** ✅（结构常量：8 个 InstancedMesh + 天空 + 地面 + 轨迹线 + 落点标记） |
| 着色器 program | ≤ 40 | **14 / 16 / 14** ✅（差值 2 = 玻璃的透明 `MeshPhongMaterial` 变体） |
| 贴图（`renderer.info` 口径） | 2 张 256×256 canvas 贴图（草 / 沙）+ 阴影贴图 | 3 ✅ |
| 几何体对象 | —— | 22（新增 `shardGeometry`） |

> ⚠️ **读 `renderer.info.render.calls` 前必须知道：它把阴影贴图那一趟也算进来了**（实测占 47%：开阴影 82 → 关阴影 43）。
> 所以「总批次」不能当环境预算用。要单独测环境占用，先把 `levelGroup` 与 `cannon` 藏起来再读。
>
> ⚠️ 上表数字用 `_art-probe.mjs` 那种「钩 `getContext` 数 WebGL draw 调用」的方式量（headless + SwiftShader 软件渲染，帧率只有 1~2fps，所以按帧取平均后**有 ±1 的抖动**，81/82 同属一档）。**别用 `WebGL2RenderingContext.prototype` 做包装** —— 会踩 `Illegal invocation`，而且 `addScriptToEvaluateOnNewDocument` 注册的脚本**会一直留在 target 上**，把页面搞成白屏（2026-09-23 踩过）。

### 6.4 最小资产包（首屏不可缺少）

| 类别 | 资产 | 生成方式 |
|---|---|---|
| 环境 | 天空 shader（三段渐变 + 光晕，**无独立日盘**）、地面斑块贴图、远景山脉、云 | 程序化 |
| 环境 | 树丛、岩石、草丛（全部 InstancedMesh） | 程序化 |
| 主体 | 倒角砖（1 段倒角挤出体）、宝石目标（八面体）、静态石台 | 程序化 |
| 主角 | 炮台（八棱底座 + 二十面体炮身 + 八棱炮管 + 黄铜炮口环 + 木轴轮） | 程序化 |
| 反馈 | 炮弹（二十面体）、碎屑（小二十面体）、**玻璃碎屑（三棱柱，见 5.3）**、彩带（薄倒角板）、轨迹虚线、落点标记（八棱盘）、炮口闪光 | 程序化 |
| UI | 卡片 / 按钮 / 滑杆 / 弹药菱形 pip / 准星 / overlay（与场景同源配色） | Tailwind + CSS |

### 6.5 HUD 遮挡预留

- 四角卡片为**不透明奶油底 + 深棕描边**，浮在场景上；卡片间隙可穿透瞄准（`pointer-events-none` 容器 + 单卡片 `auto` 保持不变）。
- 中央 40%×30% 区域内**禁止**放置常驻 HUD（仅准星与连击文字可短时进入）。
- 触屏底部控件总高 ≤ 22vh，避免与手指瞄准区（画面下半）抢空间。

## 7. 低性能降级

| 触发条件 | 降级动作 |
|---|---|
| `(pointer: coarse)` 或 `maxTouchPoints > 0`（手机/平板） | 阴影贴图 2048→1024；点缀数量 ×0.5 |
| `devicePixelRatio > 2` | 像素比上限 2（已有） |
| 组件可随时关掉 | 草丛 InstancedMesh、落点标记、云 —— 各自独立 `visible` 开关 |

## 8. 验收条件

在目标设备/模拟器、网络条件与关键交互路径中判定；证据存 `_shots/`。

| # | 检查项 | 判定方式 | 结果 |
|---|---|---|---|
| 1 | 调色板受限 | grep 源码，`Game.ts` / `App.tsx` 无临时十六进制色 | ✅ 通过 |
| 2 | 砖块可读性 | 截图：砖墙能数清层数与块数，无彩虹噪声 | ✅ 通过（`E1`） |
| 3 | 目标物可辨 | 截图：宝石目标在竖屏 412px 宽下第一眼可辨 | ✅ 通过（`E5` / `E6`） |
| 4 | 地平线三层 | 截图：能看出远 / 中 / 近三层，远景更淡更冷 | ✅ 通过（`E1` / `E3`） |
| 5 | 性能预算 | CDP 读 draw 调用与 program 数（v1.3 口径）：三角形 / 批次 / program 在 6.3 表内 | ✅ 通过（最重的 L10：153 批 / 11,683 三角 / 16 program，见 11.5） |
| 6 | 物理未受影响 | 十关开局静置自倒数全 0（含玻璃自碎 0）；L5 不自动胜利 | ✅ 通过（`node tools/check-levels.mjs` → 10/10；L1 `0/8`、L4 `0/4`、L5 `0/30`、L6 `0/18`、L7 `0/2`、L8 `0/19`、L9 `0/12`、L10 `0/28`；玻璃分布在 L2 2 块 / L5 4 / L7 4 / L8 4 / L10 6。实机侧另由 CDP 回归 42/42 复核） |
| 7 | 交互未受影响 | 鼠标 / 触控 / 键盘三路输入回归 | ✅ 通过（42 项断言，含按住瞄准、下拖降仰角、松手 −1 发、打碎玻璃后得分上涨） |
| 8 | 包体 | 单 HTML（上限 950 KB / gzip 265 KB） | ✅ 927.54 KB / gzip 254.07 KB |

## 9. 风险与降级

| 风险 | 影响 | 处置 |
|---|---|---|
| 倒角几何三角形数上升 | 中：砖块数量最多的 L5 有 52 块 | 倒角只做 1 段；实测超预算就回退到直角盒 |
| 多面体云/山数量过多 | 低 | 山的数量和半径按深度带预设，不随机刷 |
| 程序化地面贴图 `NearestFilter` 在部分 GPU 上出现摩尔纹 | 中：`证据状态：待验证` | 若出现，改为 `LinearFilter` + 降低 repeat |
| 装饰物无物理体，炮弹会穿树 | 低：可能被当成 bug | 中远景装饰只在 `\|x\| > 24` 或 `z < -40` 布点，避开射击走廊；README「已知问题」记录 |
| 雾色与实际地平线色差 | 中：接缝可见 | 雾色与天空地平线色同源（第 5.2 节），改一处必须改另一处 |

## 10. Three.js 3D 接入单（支持模块输出）

| 项 | 内容 |
|---|---|
| 版本 / 渲染器 | three 0.186，`WebGLRenderer`（antialias，`PCFSoftShadowMap`），**不用任何 Loader** |
| 资产路线 | 零运行时资产。程序化几何 + canvas 贴图，替代 glTF/GLB 路线；无网络加载、无缓存策略问题 |
| 场景层级 | `scene` → `levelGroup`（砖）/ `cannon`（炮台）/ 环境组 / `trajLine` + `landingMark`。保持 project 既有根结构不变 |
| 单位 / 轴向 | 米制；Y 上；瞄准方向为 −Z；炮台在原点，靶场在 z ≈ −22 … −32 |
| 材质通道 | 无 PBR 贴图集。`MeshPhongMaterial{ flatShading, shininess ≤ 20, specular 极暗 }` 是主家族；地面用同族 + `map`（程序化）；粒子 / 尾迹 / 彩带 / 炮口闪光 / 落点标记用 `MeshBasicMaterial`（自发光反馈）。**两个例外**：① 云用 `MeshLambertMaterial` + 强 `emissive`；② 玻璃砖用同族 `MeshPhongMaterial` 但 `transparent` + `shininess 130`（原因见第 5.3 节，另见 HANDOVER「陷阱九」） |
| 三角形 / 批次 / program 预算 | 见 6.3；已用 `renderer.info.render` 实测回填 |
| 光照 / DPR | 半球光 1 + 主平行光（带阴影）1 + 补平行光 1；DPR 上限 2；阴影贴图按设备档 2048/1024 |
| 兼容降级 | 无 WebGL 时由 React 层兜底（现状未做，`推测` 需要时再加）；资源加载失败不可能发生（零外部资源） |
| 设备证据 | `已验证`：桌面 1440×900 headless Chrome；`已验证`：移动模拟 412×915。`待验证`：真机 GPU 帧时间 |

---

## 11. 实测验收记录（2026-09-23）

跑在**正式构建产物**上（不是 dev server），headless Chrome 153 经 CDP 驱动，两档视口。

### 11.1 自动化断言

| 脚本 | 覆盖 | 结果 |
|---|---|---|
| 美术回归（20 项） | canvas 创建 / 教程页中英 / L1 弹药与开局自倒 / `renderer.info` / 暂停面板 / L4 与 L5 静置稳定 / 手机档触控说明 / 按住进入瞄准态 / 按住不减弹 / 下拖降仰角 / 松手 −1 发 / 退出瞄准态 | **20 / 20 PASS** |
| 局部取证（3 项） | 云材质归因 / 落点标记可见且在画面内 / 轨迹开关 A-B 对照 | **3 / 3 PASS** |

### 11.2 性能实测（v1.1 首测，历史记录）

> ⚠️ 这一组是 v1.1（9 关 → 10 关之前、玻璃砖之前）的数字，**已被 11.5 的重测取代**。留在这里是为了看趋势，引用请用 11.5。

| 指标 | 桌面 1440×900 | 手机 412×915 |
|---|---|---|
| 绘制批次（含阴影趟） | 81 | 73 |
| 其中环境部分 | 12 | —— |
| 三角形 | 9,694 | 6,026 |
| 着色器 program | 14 | 15 |
| 几何体对象 | 21 | 24 |

阴影趟占 38 次绘制（47%）。环境由 **8 个 InstancedMesh + 天空 + 地面** 承载，另有轨迹线与落点标记各 1 次。

### 11.3 证据截图（`_shots/`）

`E0` 教程页 · `E1` L1 默认 22° 视角 · `E2` 暂停面板 · `E3` L5 静置 · `E4` 手机教程页 · `E5` 手机 L1 · `E6` 手机瞄准态 · `D1` 落点标记放大 · `D2` 云放大。

### 11.4 扩到 10 关后的追加验收（2026-09-23 晚）

新增 L6~L10 后重跑，仍是**正式构建产物** + headless Chrome + CDP。

| 脚本 | 覆盖 | 结果 |
|---|---|---|
| 关卡自检 `tools/check-levels.mjs` | 10 关的摆放静检 + 开局空跑 6 秒 | **10 / 10 通过**（全部贴合、自倒 0 块） |
| 扩关回归 `_verify10.mjs`（25 项） | 教程页 / 选关面板 10 个按钮 / L6~L10 逐关进得去且显示「第 N 关 / 10」/ 开局静置进度为 `0 / N` / 手机 412×915 能进第 10 关 / 窄屏无横向溢出 / 无控制台报错 | **25 / 25 PASS** |
| 难度校准 `tools/playtest.mjs` | 10 关逐关扫「一发最多打掉几块」 | 见 README 难度曲线表；据结果下调了 L6 / L9 的目标数 |

新增截图：`C10-L6..L10-desktop`（五关入场） · `C10-L10-mobile` · `C10-02-levelselect-desktop` · `C10-03-levelselect-mobile`。

**这一轮对美术本身零改动** —— 新关卡只用了既有的 `PALETTE` / `mat()` / `brickGeometry()`，没有引入任何新色值、新材质或新几何体。列在这里是因为它顺带证明了「受限调色板 + 唯一材质工厂」这套约束在**结构复杂度翻倍**时依然扛得住（L10 的 66 块砖与 L1 的 27 块共用同一批缓存材质，批次却没有随砖数线性上涨）。

### 11.5 玻璃砖 + 靶场拉近后的重测（2026-09-23）

本轮**首次给场景加了新材质族**（玻璃的透明高光 Phong），所以渲染预算整表重测了一遍。仍是正式构建产物 + headless Chrome + SwiftShader 软件渲染。

| 指标 | 桌面 L1（无玻璃） | 桌面 L10（最重，含玻璃） | 手机 412×915 L1（降级档） | 预算 |
|---|---|---|---|---|
| 绘制批次（含阴影趟） | 82 | **153** | 82 | ≤ 260 / 低端 ≤ 180 ✅ |
| 三角形 | 9,739 | **11,683** | 6,399 | ≤ 120 K / 低端 ≤ 80 K ✅ |
| 着色器 program | 14 | **16** | 14 | ≤ 40 ✅ |
| 几何体对象 | 22 | 22 | 22 | —— |

**结论与依据**：

1. **玻璃让 program 数从 14 涨到 16（+2）**。原因是 `transparent: true` 会把同一份 `MeshPhongMaterial` 分到另一套 program 变体上（不透明 / 透明各一份），加上玻璃砖本身不走 `mat()`。
   `program ≤ 40` 的预算仍有一倍以上余量，**这个代价可以接受**；但如果以后再引入两三种「需要不同混合模式」的材质，这个数会线性上涨，届时要盯它。
2. **最重的关不再是 L1 而是 L10（153 批 / 11.7 K 三角）**。批次从 L1 到 L10 涨了近一倍，但**远低于 260 的上限**，而且砖块数是 66 : 27（2.4 倍）—— 批次涨幅明显小于砖数涨幅，说明共享材质批量合并没有退化。
3. **手机降级档启动正确**：三角形 9,739 → 6,399（−34%，点缀 ×0.5 生效）。批次不变（82）是因为批次由「材质数 × 阴影趟」决定，与点缀数量无关。
   ⚠️ 量这一档**必须开触控模拟**（`Emulation.setTouchEmulationEnabled`）：降级条件是 `(pointer: coarse)`，只改视口尺寸**不会**让媒体查询变成 coarse，量到的会是桌面档的数字（本次踩过）。
4. **环境部分仍是 12 批**（8 个 InstancedMesh + 天空 + 地面 + 轨迹线 + 落点标记）—— 本轮没动环境。

**证据截图**：`_shots/A-1440x900-L1.jpg`、`A-1440x900-L10.jpg`、`A-412x915-L1.jpg`；玻璃的实机表现见 `_shots/G2-L1-hint-banner` / `G3-L2-hint-banner` / `G4-L2-glass-before` / `G5-L2-after-shot` / `G6-L2-glass-zoom` / `G7-L8-glass-cols`。

**本轮美术规范的变化**（这是 v1.2 → v1.3 的实质内容，不只是数字）：

- 新增 `ENV.glass = #D6F0F5`（玻璃的混合基准色，不是新砖色位）；
- 新增 `glassMaterial(color, variant)`：砖色朝 `ENV.glass` 混 **75%** + `transparent/opacity 0.6` + `shininess 130` + `castShadow = false`；
- 新增 `shardGeometry()`（三棱柱碎屑，**故意不缓存**）；
- **`shininess ≤ 20` 这条主家族规则出现第二个例外**（玻璃 130），理由与验收方式写在第 5.3 节。

---

## 变更记录

| 版本 | 日期 | 变更 | 责任人 |
|---|---|---|---|
| v1.0 | 2026-09-23 | 首版：确立「精致 Low-Poly / 纸模微缩靶场」方向，替换原高饱和玩具风 | AI（阿光批准执行） |
| v1.1 | 2026-09-23 | 落地实现并回填实测数据：新增 `src/game/art.ts`（650 行）；修正第 5.2 节环境色表（地平线色、雾 = `skyHorizon`、无独立日盘）；新增落点标记；修正云材质策略（Lambert + emissive）；新增第 11 节实测验收记录 | AI |
| v1.2 | 2026-09-23 | 关卡扩到 10 关后追加验收：第 6 节焦点区与验收表的「五关」改为「十关」并回填 L6~L10 的开局进度；新增 11.4 节（扩关验收记录）。**美术规范本身未改** —— 新关卡完全复用既有的调色板与材质工厂。同时修掉文档里的过时引用：`COLORS` 已废止（色值只有 `art.ts` 的 `PALETTE` 一处） | AI |
| v1.3 | 2026-09-23 | **首次新增材质族**：5.3 节（玻璃砖的配色与材质规格，含「砖色朝 `ENV.glass` 混 75%」的规则、`shininess 130` 这个主家族第二例外、`castShadow=false`）；5.2 节补 `ENV.glass`；5.4 节禁例补三条（不许给玻璃写临时色值 / 不许让玻璃投影 / 独占资源例外由两处改为三处，新增 `shardGeometry()`）；6.3 与 11.5 节**整表重测并回填**（新增 worst case L10 的数据、手机降级档的实测方式、包体 927.54 KB / gzip 254.07 KB）；验收表 6/7/8 项回填 42/42 与新包体 | AI |
